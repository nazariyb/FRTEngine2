"""frtprof — FRTEngine2 profiling-session analysis package.

Import submodules directly so Jupyter ``%autoreload`` picks up edits without a
kernel restart::

    from frtprof import load, metrics, plots, tables, costmodel
"""
